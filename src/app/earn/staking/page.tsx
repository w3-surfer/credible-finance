'use client';

import { Layout } from '@/components/Layout';
import { Staking } from '@/components/dashboard';

export default function StakingPageRoute() {
  return (
    <Layout>
      <Staking />
    </Layout>
  );
} 